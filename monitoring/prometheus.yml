global:
  scrape_interval: 5s
  scrape_timeout: 2s
  evaluation_interval: 5s

scrape_configs:
  - job_name: cascaderpc
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:3000
